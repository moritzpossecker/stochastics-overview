=== Exponentialverteilung
(Berechnung der Duer zufälliger Zeitintervalle)

$ F(X) = cases(
  1 - e^(-k x) &"für" x > 0,
  0            &"sonst"
) "mit" k > 0 $

$ f(x) = cases(
  k e^(-k x) &"für" x > 0,
  0          &"sonst"
) $

$ E(X) = 1 / k $

$ sigma^2 = 1 / k^2 $

$ sigma = 1 / k $

$ x_p = -ln(1 - p) / k $

$ x_med = ln(2) / k $